apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.data }}
  labels:
    run: {{ .Values.global.data }}
spec:
  ports:
    - port: 82
      targetPort: 82
      protocol: TCP
  selector:
    app: {{ .Values.global.data }}