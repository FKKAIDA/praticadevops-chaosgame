apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.global.data }}-pvc-examples
spec:
  storageClassName: examples
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi