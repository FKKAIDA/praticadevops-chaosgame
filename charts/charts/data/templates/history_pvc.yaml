apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.global.data }}-pvc-history
spec:
  storageClassName: history
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi