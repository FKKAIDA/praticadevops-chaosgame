apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.global.data }}-pv-history
  labels:
    type: local
spec:
  storageClassName: history
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /history
    type: DirectoryOrCreate