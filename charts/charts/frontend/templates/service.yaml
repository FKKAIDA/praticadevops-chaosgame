apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.frontend }}
  labels:
    run: {{ .Values.global.frontend }}
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: {{ .Values.global.frontend }}